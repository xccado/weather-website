{
    "rewrites": [
        { "source": "/api/(.*)", "destination": "/api/weather" }
    ],
    "headers": [
        {
            "source": "/(.*)",
            "headers": [
                { "key": "Access-Control-Allow-Origin", "value": "*" }
            ]
        }
    ]
}